@model Classroom.Models.DB_Models.Subject

@{
    ViewBag.Title = "CreateSubject";
}

<h2>CreateMarks</h2>

    <div class="form-horizontal">
        <h4>Subjects</h4>
        <hr />
        @Html.ValidationSummary(true)
    @using (Html.BeginForm("CreateMarks", "Student", FormMethod.Post))
    {
  @Html.ValidationSummary()
        <div class="form-group">
            @Html.Label("Subject Name", new { @class = "col-md-2 control-label" })
            <div class="col-md-10">
                @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
            </div>
        </div>
            @*<div class="form-group">
                @Html.LabelFor(m => m.Mark, new { @class = "col-md-2 control-label" })
                <div class="col-md-10">
                    @Html.TextBoxFor(m => m.Mark, new { @class = "form-control" })
                </div>
            </div>*@
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    }
        
   </div>

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
